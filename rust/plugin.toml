id = "rust-example"
name = "Rust Example"
version = "0.0.0"
endpoint = "/examples/rust/"
component = "target/wasm32-wasip2/release/rust_example.wasm"

[build]
cmd = "cargo build --release --target wasm32-wasip2"
